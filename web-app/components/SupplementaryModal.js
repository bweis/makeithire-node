import React, { Component } from 'react';
import { getCompanyList } from '../helpers/api';

import { Grid, Header, Card, Button, Modal, Input } from 'semantic-ui-react';

export default class SupplementaryModal extends Component {
    
    constructor(props) {
        super(props);
    }
}